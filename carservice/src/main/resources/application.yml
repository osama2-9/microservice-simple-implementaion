server:
  port: 9001
  
spring:
  application:
    name: car

  datasource:
    url: jdbc:postgresql://localhost:5432/cardb?serverTimezone=UTC
    username: postgres
    password: osama
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true
    properties:
      hibernate:
        format_sql: true 
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always
