server:
  port: 9002
  
spring:
  application:
    name: reservation

  datasource:
    url: jdbc:postgresql://localhost:5432/reservationdb?serverTimezone=UTC
    username: postgres
    password: osama
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true
    properties:
      hibernate:
        format_sql: true 
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

